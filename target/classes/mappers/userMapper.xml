<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgitbank.ebs.mapper.userMapper">
<select id="getUser" parameterType="String" resultType="UserDTO">
	select * from account where id = #{userId}
</select>
<insert id="newUser" parameterType="UserDTO">
	insert into account values(#{id}, #{pw}, #{name}, #{gender}, #{birth}, #{email}, #{phoneNum}, 1, 1, 1, null,null,null)
</insert>
<update id="updateUser" parameterType="UserDTO">
	update account set schoolId = #{schoolId}, set classNum = #{classNum}, num = #{num},notice = #{notice}, sms = #{sms}  where id = #{id}
</update>
</mapper>